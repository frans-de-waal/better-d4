#Requires AutoHotkey v2.0

#HotIf WinActive("ahk_class Diablo IV Main Window Class")

  ; LButton::Send "{m down}"
  ; LButton Up::Send "{m up}"

  ; RButton::Send "{Ctrl down}{RButton down}"
  ; RButton Up::Send "{RButton up}{Ctrl up}"

  ; q::Send "{Ctrl down}{q down}"
  ; q Up::Send "{q up}{Ctrl up}"

  w::Send "{Ctrl down}{w down}"
  w Up::Send "{w up}{Ctrl up}"

  ; e::Send "{Ctrl down}{e down}"
  ; e Up::Send "{e up}{Ctrl up}"

  ; r::Send "{Ctrl down}{r down}"
  ; r Up::Send "{r up}{Ctrl up}"

  ; Space::Send "{Ctrl down}{Space down}"
  ; Space Up::Send "{Space up}{Ctrl up}"

#HotIf
